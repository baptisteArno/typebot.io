apiVersion: apps/v1
kind: Deployment
metadata:
  name: typebot-viewer
spec:
  template:
    spec:
      containers:
        - name: typebot-viewer
          resources:
            requests:
              cpu: "500m"
