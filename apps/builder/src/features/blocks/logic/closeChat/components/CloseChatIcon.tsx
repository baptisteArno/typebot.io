import { CloseChat } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const CloseChatIcon = (props: IconProps) => (
  <CloseChat color="purple.500" {...props} />
)
