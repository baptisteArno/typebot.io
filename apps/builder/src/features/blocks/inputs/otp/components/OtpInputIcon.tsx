import { UnlockedIcon } from "@/components/icons";
import type { IconProps } from "@chakra-ui/react";
import React from "react";

export const OtpInputIcon = (props: IconProps) => (
  <UnlockedIcon color="orange.500" {...props} />
);
