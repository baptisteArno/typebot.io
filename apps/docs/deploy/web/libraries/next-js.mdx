---
title: Next.js
---

This lib is a convenient wrapper around the [Sniper React](./react) library. It makes sure the components are lazy loaded.

## Install

```bash
npm install @sniper.io/js @sniper.io/nextjs
```

## Usage

Use it like you would use the [Sniper React](./react) library.
