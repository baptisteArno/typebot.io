{
  "baseBranch": "origin/main",
  "pipeline": {
    "lint": {
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "build/**"],
      "outputMode": "new-only"
    },
    "db#build": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["^dev"],
      "cache": false
    }
  }
}
