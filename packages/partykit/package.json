{
  "name": "@typebot.io/partykit",
  "scripts": {
    "dev": "partykit dev --live",
    "deploy": "partykit deploy",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "partykit": "^0.0.110",
    "@typebot.io/tsconfig": "workspace:*"
  }
}
