import { Link04Icon } from "@hugeicons-pro/core-stroke-rounded";
import { Icon } from "../components/Icon";

export const LinkIcon = ({ className }: { className?: string }) => (
  <Icon icon={Link04Icon} className={className} />
);
