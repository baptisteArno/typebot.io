import { Delete02Icon } from "@hugeicons-pro/core-stroke-rounded";
import { Icon } from "../components/Icon";

export const TrashIcon = ({ className }: { className?: string }) => (
  <Icon icon={Delete02Icon} className={className} />
);
